import React, { useState, useEffect, useContext } from 'react';
import OrderSummary from '../../layouts/OrderSummaty';

const Checkout = () => {
    useEffect(() => {
    }, []);

    return (
        <>
            <OrderSummary />
        </>
    );
};

export default Checkout;