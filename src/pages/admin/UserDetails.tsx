import React, { useState, useEffect, useContext } from 'react';

const UserDetails = () => {
    useEffect(() => {
    }, []);

    return (
        <>
        </>
    );
};

export default UserDetails;